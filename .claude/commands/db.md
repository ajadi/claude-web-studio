---
description: Design DB schema, plan migrations, optimize queries — run parallel with architect for DB-heavy tasks
argument-hint: "[schema or query to design/optimize]"
---

$ARGUMENTS is an optional description of the DB design task.

Use the database-architect agent for: schema design, migration planning, query optimization, index strategy.

For DB-related tasks, run in parallel with the architect agent.
